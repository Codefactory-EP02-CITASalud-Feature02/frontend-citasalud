// Copy this file to 'public/env.js' (or create a file named 'env.js' in your static root at deploy time)
// and update the VITE_API_URL to point to your deployed backend GraphQL endpoint.
// Example: window.__env = { VITE_API_URL: 'https://backend-citasalud.fly.dev/graphql' };

window.__env = window.__env || {};
window.__env.VITE_API_URL = 'https://backend-citasalud.fly.dev/graphql';
